_BASE_: "../fastreid/configs/VeRi/sbs_R50-ibn.yml"


MODEL:
    # --- 关键修改：禁止自动下载预训练权重 ---
  DEVICE: "cuda"

SOLVER:
  AMP:
    ENABLED: False
  IMS_PER_BATCH: 32

DATALOADER:
  NUM_INSTANCE: 8

TEST:
  IMS_PER_BATCH: 64

OUTPUT_DIR: "outputs/veri_r50ibn_sbs_s0_v1_20260410"
