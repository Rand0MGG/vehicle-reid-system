_BASE_: "../fastreid/configs/VeRi/sbs_R50-ibn.yml"

SEED: 3

MODEL:
  BACKBONE:
    NAME: build_resnet_lka_backbone
    WITH_NL: True
    LKA:
      ENABLED: True
      STAGES: ("layer4",)
      ALPHA: 0.025
      LR_FACTOR: 1.0
      FREEZE_WITH_BACKBONE: False
      OUTPUT_MODE: fixed_residual
    HCA:
      ENABLED: False

SOLVER:
  AMP:
    ENABLED: True
  IMS_PER_BATCH: 64
  MAX_EPOCH: 60

DATALOADER:
  NUM_INSTANCE: 16

TEST:
  EVAL_PERIOD: 5
  IMS_PER_BATCH: 128

OUTPUT_DIR: "outputs/veri_r50ibn_lka_s7b_alpha0025_nl_amp64_seed3_v1_20260417"
