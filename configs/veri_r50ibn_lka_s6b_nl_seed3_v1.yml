_BASE_: "../fastreid/configs/VeRi/sbs_R50-ibn.yml"

SEED: 3

MODEL:
  BACKBONE:
    NAME: build_resnet_lka_backbone
    WITH_NL: True
    LKA:
      ENABLED: True
      STAGES: ("layer4",)
      GAMMA_INIT: 1e-6
      LR_FACTOR: 2.0
      FREEZE_WITH_BACKBONE: False
      OUTPUT_MODE: gated_residual
    HCA:
      ENABLED: False

SOLVER:
  AMP:
    ENABLED: True
  IMS_PER_BATCH: 64
  MAX_EPOCH: 60

DATALOADER:
  NUM_INSTANCE: 16

TEST:
  EVAL_PERIOD: 5
  IMS_PER_BATCH: 128

OUTPUT_DIR: "outputs/veri_r50ibn_lka_s6b_nl_amp64_seed3_v1_20260416"
